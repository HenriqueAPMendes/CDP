#include <bits/stdc++.h>
using namespace std;

#define MAX 1123

int main (){
    cin.tie(0); cout.tie(0);
    ios_base::sync_with_stdio(0);

    int H, C, S, P;
    double W;

    cin >> H >> C >> S >> P >> W;
    cin >> 
}